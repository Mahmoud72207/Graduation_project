# Graduation_project
This project aims to analyze the sentiment of text using the BERT model and then convert the text into speech with a voice that dynamically changes based on the detected sentiment (negative, neutral, positive).
